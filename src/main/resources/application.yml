spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/atongtest?characterEncoding=UTF-8
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: "123456"

  flyway:
    locations:
      - "classpath:/db"
server:
  port: 8091

profiles:
  active: "@profiles.active@"